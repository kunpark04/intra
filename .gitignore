# Large data files (>100MB — exceeds GitHub limit)
data/NQ_1min.csv

# Sweep logs (generated; re-run param_sweep.py to reproduce)
logs/
sweep_run*.log

# ML sweep datasets (large generated files; live in data/ml/)
data/ml/**
# Exception: small ranker-null overlap labels (13,814 rows, ~224 KB) — load-bearing for Phase 4.1 finding
!data/ml/ranker_null/
!data/ml/ranker_null/combo_overlap_labels.parquet
# Exception: Probe 3 gate artifacts (~810 KB total JSON + parquet) — load-bearing for probe3_verdict.md reproducibility at signing commit 8636167
!data/ml/probe3/
!data/ml/probe3/**
# Exception: Probe 4 gate artifacts (~160 KB total JSON + parquet) — load-bearing for probe4_verdict.md reproducibility
!data/ml/probe4/
!data/ml/probe4/**

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/
.env

# Jupyter
.ipynb_checkpoints/
*.ipynb~

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code local config (not project-specific)
.claude/

# Generated artifacts (large; re-run notebooks/scripts to produce)
iterations/*/equity_curve.csv
evaluation/equity_curve.csv

# Parquet intermediates
*.parquet
!data/ml/ranker_null/combo_overlap_labels.parquet
!data/ml/probe3/**/*.parquet
!data/ml/probe4/**/*.parquet

# Cython build artifacts
*.pyd
*.so
src/cython_ext/*.c
build/

# Keep iteration/evaluation structure
# (trades.csv, daily_ledger.csv, etc. are small enough to commit)
